const account_number=608
let customer_name="shivam"
branch_name="babra"
console.table([account_number,customer_name,branch_name])
//account_number=221 not change because constant not change
customer_name="mayur"
branch_name="rajkot"
console.table([account_number,customer_name,branch_name])
//var is not use because scop problem
let a;
console.log(a);//undefine